# BITOL dataset files

Images, labels, class names, and the original dataset configuration remain in this directory.

See [the dataset documentation](../dataset/README.md) for counts, annotation format, provenance, and known limitations. The original `data.yaml` is historical evidence and aliases validation to test; the publication workflow uses [configs/data.yaml](../configs/data.yaml), which leaves the unavailable validation protocol explicit.
